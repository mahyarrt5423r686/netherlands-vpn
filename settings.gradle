pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // برای libv2ray
        flatDir {
            dirs("app/libs")
        }
    }
}
rootProject.name = "Netherlands VPN"
include(":app")
